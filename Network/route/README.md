# Qroute

Read-only display of the IPv4 route table through Microware NetDB.
The historical `-n` option is accepted; Qroute already prints numeric
addresses and therefore does not need a separate name-resolution path.
Writing routes is intentionally not enabled; use Qroutectl for guarded
route updates.
