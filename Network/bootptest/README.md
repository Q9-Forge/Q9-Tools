# Qbootptest

Sendet ein einzelnes BOOTP-Request-Paket an einen angegebenen Server oder an
die IPv4-Broadcastadresse. Es ist bewusst ein Diagnosewerkzeug und wartet
noch nicht auf bzw. dekodiert noch keine BOOTP-Antwort.
