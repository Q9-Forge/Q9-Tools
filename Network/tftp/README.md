# Qtftp

Grundlegender RFC-1350-TFTP-Client im `octet`-Modus. Unterstützt `get` und
`put` mit Host, Remote- und lokalem Dateinamen. Timeout, Retransmit und
Option-Extension werden in der nächsten Iteration ergänzt.
