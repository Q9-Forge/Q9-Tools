# Qtouch

Microware-C-Port der OS-9-Datei-Zeitpflege. Die RBF-Dateideskriptor-
Schnittstelle wird direkt über `_os_gs_fd` und `_os_ss_fd` angesprochen.
Die Listenoptionen `-z` und die zentrale Execution-Directory-Auflösung sind
noch offen; `-c`, `-q` und `-?` sind enthalten.
