# Qpr

Microware-C-Nachbau des OS-9-Programms `pr`. Der Modulname bleibt `pr`, die
ausgelieferte Datei heißt `Qpr`.

Unterstützt werden die Grundfunktionen mit Seitenüberschrift, `-t`, `-f`,
`-p=num`, `-u=title`, `-z` und `-z=<path>`.
