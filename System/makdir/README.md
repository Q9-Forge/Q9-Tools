# Qmakdir

Microware-C-Nachbau des OS-9-Programms `makdir`. Der Modulname bleibt
`makdir`, die ausgelieferte Datei heißt `Qmakdir`.

Unterstützt werden `-?`, `-p`, `-q`, `-z` und `-z=<file>`. Die Erstellung
erfolgt über die native Microware-C-Funktion `makdir()`.
