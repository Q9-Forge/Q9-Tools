# Qrename

Microware-C-Nachbau des OS-9-Programms `rename`. Der Modulname bleibt
`rename`, die ausgelieferte Datei heißt `Qrename`.

Unterstützt werden `-?`, zwei Dateinamen und die kompatible Option `-x`.
