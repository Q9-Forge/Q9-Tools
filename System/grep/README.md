# Qgrep

Microware-C-Nachbau des OS-9-Programms `grep`. Der Modulname bleibt `grep`,
die ausgelieferte Datei heißt `Qgrep`.

Unterstützt werden einfache Zeilensuche, `-c`, `-i`, `-l`, `-n`, `-v`, `-z`
und `-z=<path>`.
