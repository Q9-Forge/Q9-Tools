# Qlist

Microware-C-Nachbau des OS-9-Programms `list`. Der Modulname bleibt `list`,
die ausgelieferte Datei heißt `Qlist`.

Unterstützt werden Dateiausgabe, mehrere Dateinamen, `-?`, `-z` und
`-z=<path>`. `-x` wird als kompatible Option akzeptiert.
