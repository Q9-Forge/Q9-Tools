# Qcmp

Microware-C-Nachbau des OS-9-Programms `cmp`. Der Modulname bleibt `cmp`,
die ausgelieferte Datei heißt `Qcmp`.

Vergleicht zwei Dateien byteweise. Unterstützt werden `-?` und `-t` für die
kompakte Byte-Statistik.
