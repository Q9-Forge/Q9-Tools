# Qtr

Microware-C-Nachbau des OS-9-Programms `tr`. Der Modulname bleibt `tr`, die
ausgelieferte Datei heißt `Qtr`.

Unterstützt Zeichenübersetzung, `-d` zum Löschen von Zeichen, `-c` für die
Komplementmenge und optionale Eingabe-/Ausgabedateien.
