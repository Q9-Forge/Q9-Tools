# Qqsort

Q9-Nachbau des OS-9-`qsort` für zeilenorientierte Dateien. Unterstützt `-f`,
`-c`, `-z` und `-?`. Die Eingabe wird im Speicher gesammelt und anschließend
sortiert; maximal 2048 Zeilen pro Aufruf.
