# Qcount

Microware-C-Nachbau des OS-9-Programms `count`. Der Modulname bleibt
`count`, die ausgelieferte Datei heißt `Qcount`.

Unterstützt werden Zeilen-, Wort- und Zeichenzählung mit `-l`, `-w`, `-c`, die
Gruppierung über `-d[=]<n>`, sowie Dateilisten über `-z` und `-z=<path>`.
